# Clé API CoinMarketCap — copier ce fichier en .env et renseigner la valeur.
# Obtenir une clé : https://pro.coinmarketcap.com/account
# Le .env réel n'est jamais versionné (voir .gitignore) ; la clé n'est lue
# que côté serveur par proxy.conf.js, jamais exposée au navigateur.
CMC_API_KEY=
